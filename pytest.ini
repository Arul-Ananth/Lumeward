[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    live: requires network, API keys, or external services
    slow: long-running test
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated.*:Warning
